name: class_flutter_1_todo
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.6.0

dependencies:
    flutter:
        sdk: flutter
    flutter_bloc: ^8.1.6
    flutter_svg: ^2.0.16
    fpdart: ^1.1.1
    get_it: ^8.0.3
    supabase_flutter: ^2.8.2

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: true

    assets:
        - assets/svgs/
